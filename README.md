# Mini Web Application Firewall (WAF)

- Detects SQL Injection (SQLi) and XSS attacks
- Built using Python and Flask
- Implements rule-based filtering and logging
- Includes IP blocking mechanism

## Run
pip install -r requirements.txt
python backend.py
python waf.py
